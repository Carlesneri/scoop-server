name: Get and insert sport news
on:
  schedule:
    - cron: "15 */3 * * *"
  workflow_dispatch: 
jobs:
  get-insert-news:
    uses: ./.github/workflows/reusable-get-news.yml
    with:
      file-name: get-and-insert-sport.ts